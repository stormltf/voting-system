# Docker 部署环境变量配置
# 复制此文件为 .env 并修改配置

# MySQL 配置
MYSQL_ROOT_PASSWORD=voting123
MYSQL_DATABASE=voting_system
MYSQL_USER=voting
MYSQL_PASSWORD=voting123

# JWT 配置（生产环境请修改为随机字符串）
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# 前端 API 地址
# 本地部署使用 http://localhost:3001/api
# 服务器部署使用 http://your-server-ip:3001/api
NEXT_PUBLIC_API_URL=http://localhost:3001/api
